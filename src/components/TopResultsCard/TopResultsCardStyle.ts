import styled from "styled-components/native";
import { theme } from "../../styles/styles";

export const GoldenPlaceholder = styled.View`
  align-items: center;
  background-color: ${theme.bgLighter};
  padding: 10px;
  border-radius: 10px;
`;
