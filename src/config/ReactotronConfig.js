// import AsyncStorage from "@react-native-async-storage/async-storage";
// import { NativeModules } from "react-native";
// import reactotron from "reactotron-react-native";
// import { reactotronRedux } from "reactotron-redux";

// const { scriptURL } = NativeModules.SourceCode;
// const hostName = scriptURL.split("://")[1].split(":")[0];

// reactotron
//   .setAsyncStorageHandler(AsyncStorage)
//   .configure({ host: hostName }) // AsyncStorage would either come from `react-native` or `@react-native-community/async-storage` depending on where you get it from // controls connection & communication settings
//   .useReactNative() // add all built-in react native plugins
//   .connect() // let's connect!
//   .use(reactotronRedux());

// console.tron = reactotron;

// export default reactotron;
